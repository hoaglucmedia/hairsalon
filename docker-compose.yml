version: "3.8"

services:
  web:
    build: .
    container_name: php56_web
    ports:
      - "8080:80"
    depends_on:
      - db

  db:
    image: mysql:5.7
    container_name: mysql5_db
    ports:
      - "3309:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root

